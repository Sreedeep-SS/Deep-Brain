Implement a Python + Celery + Redis task
“How do retries work?” → Set retry=True “What broker and result backend do you use?” → Usually Redis or RabbitMQ
Just practice implementing:

Key → value pair

Round robin assignment with INCR

TTLs (Time-to-Live)